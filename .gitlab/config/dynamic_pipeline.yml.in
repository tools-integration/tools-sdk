default:
  id_tokens:
    OLCF_ID_TOKEN:
      aud: https://code.olcf.ornl.gov

child_pipeline_{branch}:
  variables:
    DOWNSTREAM_COMMIT_SHA: '{commit}'
    DOWNSTREAM_BRANCH_REF: '{branch}'
  trigger:
    include:
      - project: 'ci/ums032/dev/tools-sdk'
        ref: '{branch}'
        file: '.gitlab/gitlab-ci-frontier.yml'
