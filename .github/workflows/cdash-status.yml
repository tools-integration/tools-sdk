name: 'CDash Status'
on:
  push:
  pull_request_target:

jobs:
  cdash-status:
    runs-on: ubuntu-latest
    steps:
      - uses: Kitware/cdash-status@v1
        with:
          project: "TOOLS-SDK"
